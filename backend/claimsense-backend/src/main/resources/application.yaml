spring:
  application:
    name: claimsense-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/claimsense
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

